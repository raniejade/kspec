apply from: "$rootDir/gradle/common/dependencies.gradle"
apply from: "$rootDir/gradle/common/kotlin.gradle"

dokka {
    outputFormat = 'html'
}

dependencies {
    compile project(":core")
    compile project(":junit-runner")

    testCompile "com.natpryce:hamkrest"
}
